version: 1.0
runtime: python3
build:
  commands:
    build:
      - python -m pip install -r requirements.txt
run:
  command: python -m gunicorn --bind 0.0.0.0:8080 main:app
  network:
    port: 8080
