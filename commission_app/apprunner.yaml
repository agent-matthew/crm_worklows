version: 1.0
runtime: python3
build:
  commands:
    build:
      - python3 -m pip install -r requirements.txt
run:
  command: sh -c "export PATH=$PATH:$(python3 -c 'import sysconfig; print(sysconfig.get_path(\"scripts\"))') && gunicorn --bind 0.0.0.0:8080 main:app"
  network:
    port: 8080
