version: 1.0
runtime: python3
build:
  commands:
    build:
      - python3 -m pip install -r requirements.txt
run:
  command: python3 -m gunicorn --bind 0.0.0.0:8080 main:app
  network:
    port: 8080
